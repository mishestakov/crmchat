# BD-тест: задачи из сессии с Юлей (19.06.26)

Источник: расшифровка тест-сессии BD-отдела. Идём по списку сверху вниз.

## Активные

- [x] **Убрать «создано каналов N»** из модалки добавления каналов — пользователю
  ничего не даёт (это find-or-create в справочнике `channels`, внутренняя кухня).
  Сделано: `add-channels-modal.tsx`. Поле `channelsCreated` в ответе ручки оставлено.

- [x] **Баг: каналы сами уходят из инбокса «нет контактов» в «в работе» без контакта.**
  Исчезали без кликов оператора, всплывали как «в работе» с пустым контактом.

  **Причина (подтверждена Codex по prod-БД + код):** статус не хранится, считается
  на лету. Предикат «контакт готов» (`contact-sql.ts`) засчитывал готовым любой
  канал, у которого просто ЕСТЬ бесплатная личка (`direct_messages_chat_id` из
  sync). А открытие карточки канала автоматически (без клика, TTL 24ч) запускало
  Telegram-sync, который эту личку и находил → лид молча становился «готов» →
  вылетал из «нет контактов» в «в работе». Отправки при этом нет (ничего не
  планирует) → лид в лимбо. Каскад: список перестраивается → открывается следующая
  карточка → её sync → опять (отсюда «список прыгает», скачок счётчика без кликов).

  **Фикс (сделано):** убрал авто-ветку «есть бесплатная личка» из `contactReadySql`.
  Теперь готовность держится только на явном выборе оператора: @username админа,
  привязанный контакт, или явно выбранный способ связи (`contact_method.kind`,
  включая «Использовать личку канала»). Простой просмотр карточки больше ничего не
  переклассифицирует → каскад/прыжки уходят вместе с багом. Файлы: `contact-sql.ts`,
  комментарий в `placement-recipient.ts`.

  **Осталось отдельной задачей (НЕ багфикс):** клик «Использовать личку канала»
  сейчас делает лид готовым, но НЕ ставит в очередь авто-рассылки — планировщик
  требует `username`, воркер шлёт только живому tg-юзеру. Авто-отправка опенера в
  личку канала в пайплайне не построена (только ручная отправка через чат лички).
  Если нужна авто-рассылка в личку — это фича: добавить DM-таргет в планировщик
  (`scheduleUnscheduledLeads`/`scheduleDolivkaForChannel`) и в воркер.

  **Отдельный смежный риск (не этот баг, на будущее):** входящее сообщение ставит
  `repliedAt` на ВСЕ `projectItems` с данным `tgUserId` по всему воркспейсу
  (`outreach-listener.ts`), а `repliedAt` проверяется раньше `no_contact`. Может
  флипать лиды, у которых висит `tgUserId` без полноценного контакта. Пока не
  воспроизведено в этой сессии — держим на радаре.

- [x] **Смена контакта по запросу админа** («написали админу — он говорит: пишите
  туда»). Две точки входа: кнопка «Сменить контакт» в карточке канала
  (`AdminsSection` → `ContactResolver`) и шорткат на чипе канала в шапке чата
  (иконка ⇄ → `ChannelDrawer` в режиме `contactChange`). Бэкенд уже был готов:
  `set-admin` override → `repointPlacementSchedule` перенаводит размещения на
  новый контакт, не трогая стадию/ответы лида (проверено сквозной трассировкой).

- [ ] **Кулдаун / блок аккаунта — продуктовая проработка.** Нужно продумать целиком:
  - пауза / «отдых» аккаунта на N дней (не возврат в строй по кулдауну);
  - ручное увеличение кулдауна (сутки / двое / пятеро);
  - после запуска проекта аккаунт нельзя поменять — нужна возможность убрать/не
    использовать заблокированный;
  - подсказка «пишите с обычного Telegram» (через мессенджер отправка работает,
    через сервис — нет);
  - [x] **кулдаун НЕ гейтит ручную отправку** — решение менеджера. Подтверждено:
    блокировал наш внутренний 429 (`quick-send.ts:221`), а не Telegram (PEER_FLOOD =
    антиспам только на новых; старым TG слать даёт). Убрал гейт из quick-send; авто-
    воркер кулдаун соблюдает (там нет человека); banned/unauthorized — жёсткий стоп.

- [ ] **Пометка аккаунта в кулдауне + «от кого пишем».**
  - [x] в драйвере чата было «написать через Mike», но все аккаунты — Mike.
    `formatAccount` теперь показывает `@username` первым (уникален).
  - [x] **состояние аккаунта в селектах** — единый хелпер `lib/account-health.ts`
    (`accountHealth` ok/cooldown/banned + `accountHealthDotClass`). Покрыто:
    - драйвер чата: цветной дот на табах аккаунтов + баннер над полем ввода с
      причиной (PEER_FLOOD/FloodWait/бан), не блокирует;
    - выбор аккаунтов проекта (`projects/.../accounts.tsx`): дот + метка «отлёжка»
      с причиной (бан/разлогин туда и так не попадают — отфильтрованы по active).
  - [ ] опционально: мягкое предупреждение при запуске кампании (`LaunchModal`)
    — не «селект», отдельная плашка (пул проекта × здоровье), пока не делал.

- [x] **Убрать лишнюю карточку-страницу лида → боковой чат.** Клик по канбан-карточке
  теперь открывает боковой чат (раньше — переход на страницу контакта). Доп.инфо
  уже в чате: пометка об админе (`ContactNote`), чипы каналов админа с листанием,
  и новый стрип статуса. Полная страница контакта НЕ удалена — остаётся вью для
  контакт-менеджмента (активность, связь, привязка каналов, edit/delete),
  доступна ссылкой «Открыть карточку» из шапки чата (escape-hatch).

- [ ] **Счётчик отправок новым адресатам в списке аккаунтов** (мысль, потом расширим).
  В списке outreach-аккаунтов показывать, сколько опенеров ушло новым адресатам
  (первое касание, не ответы в существующих тредах) — сигнал близости к дневному
  лимиту TG. Дальше — фиксировать эмпирику здоровья аккаунта: на каком количестве
  пошла «динамика» и при каком счётчике поймали кулдаун/бан, чтобы выводить
  безопасный порог per-account. Связано с задачами про кулдаун выше.
  - расширение: собирать стату «отправлено сегодня» и «за последние 30 дней» +
    сколько отправок привели к бану, и автоматически двигать цифру лимита
    туда-сюда (адаптивный per-account порог).

- [ ] **Прокси для аккаунтов.** 45 аккаунтов с одного IP — стрёмно. Пора добавлять
  прокси (по идее per-account или пул).

- [ ] **База «кто уже с нами работает» + привязка.** Завести базу каналов/админов,
  которые уже работают с нами, и уметь привязывать к ним лиды (чтобы не писать
  повторно). Связано с существующим `channelAlreadyWorking`.

- [x] **Открытие чата из канбан-карточки без цифры непрочитанных.** Клик по любому
  месту карточки открывает чат, не только по бэйджу. Бэйдж непрочитанных стал
  чистым индикатором (убрана отдельная кнопка).

- [x] **Селектор статуса прямо в чате.** Стрип «Статус» в шапке чата — выпадашка
  стадий проекта (+ «Без стадии»), смена через ту же `move`-мутацию канбана
  (оптимистика общая). Ответили «уже работаю» → ставишь стадию из чата, карточку
  руками не таскаешь. Стадия = свойство лида (не контакта), поэтому управление
  живёт в `LeadChatDrawer`, а `ChatDrawer` получает её слотом `headerExtra`.
  При завершённом проекте селектор disabled.

- [ ] **Авто-пинг зависших диалогов (основная рутина → автоматика).** Начали общение
  и блогер пропал. Если завис в каком-то статусе и молчит N (грубо — сутки) →
  автоматом дослать пинг/«котика»/достукивание. Пинг и достукивание до блогера —
  главная рутина, которую хочется заменить автоматикой. Продуктовые кейсы (какие
  статусы, какие тайминги, что слать) — собрать отдельно.

## Отложено (поймать/обсудить позже)

- Новое сообщение не подтягивается / висевшее «новое» пропадает из инбокса —
  поймать точнее в следующий раз.
- Массовая рассылка по доске / стандартная цепочка для «первого касания».
- Автоматизация копипасты первого оффера (дальше — живое общение, не трогаем).
- Пометка «написали в ВК / e-mail / др. канал» для лидов без TG-контакта.
- Звёзды (10⭐ за сообщение) — ждём добро от Ксюши.
- **Шаблоны сообщений + AI (большая вещь, продуктово обсуждать).** Шаблоны/примеры
  сообщений на уровне воркгруппы, AI-подстановка в шаблон, AI-перемещение карточек
  по канбану. Запарковано — требует отдельного продуктового обсуждения.
