__pycache__/
*.pyc
.venv/
venv/

# raw network dump — 45 MiB, fully reproducible via download_complete.py
crmchat_dump_full/

node_modules/
dist/
.turbo/
.env
*.tsbuildinfo

# TDLib per-account binlog/files — persistent FS state, в проде volume
apps/api/.td-database/

# TDLib API schema (downloaded artifact, не наш код)
/td_api.tl

# playground/tdlib-bridge artefacts — locally-generated, не репро
playground/tdlib-bridge/td-database*
playground/tdlib-bridge/trace-*.log
playground/tdlib-bridge/clean-*.log

# generated typed REST-client (regenerated by `pnpm -F @repo/api-client generate`)
packages/api-client/src/schema.ts

# generated TanStack Router routeTree (regenerated by vite plugin)
apps/web/src/routeTree.gen.ts

# downloaded app bundle — reproducible via download_complete.py
locales/

# local Claude Code state
.claude/

# capture artifacts — derived from raw/ via `npm run rebuild`
tools/capture/processed/

# raw CDP session dumps — not reproducible without re-running a capture session,
# but too heavy (~100MB) for git. Archive separately if needed.
tools/capture/raw/

# capture session checklist — personal state
tools/capture/state.json

# coverage report output — regenerated by `npm run coverage`
coverage.ui-gaps*.txt

# mkdocs
_site/
specs/_screenshots
