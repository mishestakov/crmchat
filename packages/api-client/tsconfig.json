{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  // scripts/ не типизируем отсюда: generate.ts импортирует apps/api-граф
  // (свой tsconfig, свои ambient-декларации типа lz4js.d.ts) — его проверяет
  // typecheck самого api. Здесь только клиент.
  "include": ["src"]
}
