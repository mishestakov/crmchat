# Create campaign (/docs/api/campaigns/outreach.sequences.create)

Creates an outreach campaign for an existing list and triggers list processing. A list must be created first using the lists endpoints.

<OpenAPIOperation
  spec="https://api.crmchat.ai/v1/spec.json"
  operation={{"method":"post","path":"/workspaces/{workspaceId}/outreach/sequences","tags":["Campaigns"]}}
/>
